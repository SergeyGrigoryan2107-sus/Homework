<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Дополнительные стили */
        form {
            max-width: 500px;
            margin: 50px auto;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 10px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        label {
            display: block;
            margin-top: 10px;
        }
        input[type="text"],
        input[type="number"] {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        .form-group {
            position: relative;
        }
        .icon {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            right: 10px;
            z-index: 1;
            pointer-events: none;
        }
        .icon img {
            width: 20px;
            height: 20px;
        }
        button {
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0056b3;
        }
    </style>
    <title>Форма оплаты картой</title>
</head>
<body>

<div class="container mt-5">
    <h2 class="mb-4 text-center">Оплата банковской картой</h2>
    <form action="#" method="post">
        <!-- Номер карты -->
        <div class="form-group mb-3">
            <label for="card-number">Номер карты:</label>
            <input type="text" id="card-number" placeholder="XXXX XXXX XXXX XXXX" required />
            <span class="icon"><img src="credit-card-icon.png" alt="Credit Card Icon"></span>
        </div>

        <!-- Имя владельца -->
        <div class="form-group mb-3">
            <label for="cardholder-name">Имя владельца:</label>
            <input type="text" id="cardholder-name" placeholder="Иван Иванов" required />
            <span class="icon"><img src="user-icon.png" alt="User Icon"></span>
        </div>

        <!-- Срок действия -->
        <div class="form-group mb-3">
            <label for="expiry-date">Срок действия:</label>
            <input type="text" id="expiry-date" placeholder="ММ/ГГ" required />
            <span class="icon"><img src="calendar-icon.png" alt="Calendar Icon"></span>
        </div>

        <!-- CVV/CVN -->
        <div class="form-group mb-3">
            <label for="cvv-code">CVV / CVN:</label>
            <input type="password" id="cvv-code" placeholder="***" required />
            <span class="icon"><img src="lock-icon.png" alt="Lock Icon"></span>
        </div>

        <!-- Кнопка отправки -->
        <button type="submit" class="btn btn-primary w-100">Оплатить</button>
    </form>
</div>

<!-- Подключаем bootstrap JS и поппер для некоторых компонентов -->
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>

</body>
</html>
